import re
from xmlrpc.client import Boolean


# A prime number (prime) is an integer greater than 1 whose only factors are 1 and itself.
def is_prime(x: int) -> Boolean:
    is_prime = False
    if x > 1:
        
    return is_prime
   

# A circular prime number is a prime number that remains prime on any cyclic rotation of its digits in base 10.
def is_circular_prime(x: int) -> Boolean:
    is_circular_prime = False
    return is_circular_prime

def main():

main()